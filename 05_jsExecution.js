/* 
JavaScript Execution Context

{code} -> Global execution context <- assign to this keyword

1. Global execution context
2. function execution context
3. Eval execution context // not that imp

? Phases of JS Code execution
1. Global execution context <- assign to this
2. Memory Creation Phase -> memory allocated(placed only) to the variables, func's (undefined)
3. Execution Phase -> executed the func's

! When function is called it create separate 'new variable environment + execution thread' in which 2nd and 3rd process repeat for each function (separate sand box) and delete after work done
*/